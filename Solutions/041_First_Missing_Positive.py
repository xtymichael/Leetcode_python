class Solution:
    # @param {integer[]} nums
    # @return {integer}
    def firstMissingPositive(self, nums):
    	# if not nums:
    	# 	return 1
     #    for i in sorted(nums):
     #    	if 
     #    	if i >= 0 and i - 1 not in nums:
     #    		return i - 1
     	if nums = [1]:
     		return 2
     	if len(nums) = 1:
     		return 1
     	lambda x, y: if x(x >=0 and y-x == 2)